<UserControl x:Class="PlayWithMahApps.Metro.Views.UserControl5"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlayWithMahApps.Metro.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <Style BasedOn="{StaticResource {x:Type mah:MetroHeader}}" TargetType="{x:Type mah:MetroHeader}">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="mah:HeaderedControlHelper.HeaderMargin" Value="0 0 0 5" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <TabControl x:Name="HamburgerTabControl"
                    Margin="10"
                    mah:TabControlHelper.Underlined="TabPanel">
            <TabItem Header="Default">
                <local:UserControl6 DataContext="{Binding}" />
            </TabItem>
            <TabItem Header="Creators Style">
                <local:UserControl6 DataContext="{Binding}" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
